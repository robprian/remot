# Normalize line endings across platforms.
* text=auto eol=lf

# Windows scripts must stay CRLF.
*.bat text eol=crlf

# The Gradle wrapper script must stay LF to remain executable on Unix.
gradlew text eol=lf

# Binaries.
*.jar binary
*.png binary
*.jpg binary
*.keystore binary
*.jks binary
